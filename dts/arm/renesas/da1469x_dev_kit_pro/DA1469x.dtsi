#include <dt-bindings/gpio/gpio.h>
#include <mem.h>
#include <arm/armv8-m.dtsi>

// sysclk: system-clock {
// 	compatible = "fixed-clock";
// 	clock-frequency = <25000000>;
// 	#clock-cells = <0>;
// };

/ {

	cpus {
		#address-cells = <1>;
		#size-cells = <0>;

		cpu@0 {
			device_type = "cpu";
			compatible = "arm,cortex-m33";
			reg = <0>;
			#address-cells = <1>;
			#size-cells = <1>;
		};
	};

	sram0: memory@20000000 {
		compatible = "mmio-sram";
		reg = <0x20000000 DT_SIZE_K(512)>; //TODO: leave space AREA_OVERHEAD
	};

	
	soc {
		flash0: flash@0 {
			compatible = "soc-nv-flash";
			reg = <0 DT_SIZE_K(384)>; 
		};

		gpio1: gpio@50020A98 {
			compatible = "renesas,DA1469x-gpio";
			reg = <0x50020A98 0x58>;
			gpio-controller;
			label = "GPIO1";
			#gpio-cells = <2>; 
		};
	};
};

&nvic {
	arm,num-irq-priority-bits = <4>;
};