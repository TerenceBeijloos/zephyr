description: DA1469x GPIO node

compatible: "renesas,DA1469x-gpio"

include: [gpio-controller.yaml, base.yaml]

properties:
    reg:
      required: true

    label:
      required: true

    "#gpio-cells":
      const: 2

    port:
      type: int
      required: true

gpio-cells:
  - pin
  - flags